name: "Test"
on:
  pull_request:
  push:
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: cachix/install-nix-action@v9
    - uses: cachix/cachix-action@v6
      with:
        name: idempotent-desktop
        signingKey: '${{ secrets.CACHIX_SIGNING_KEY }}'
    - run: sudo nix-channel --add https://nixos.org/channels/nixos-20.03 stable
    - run: sudo nix-channel --add https://nixos.org/channels/nixos-unstable nixos
    - run: sudo nix-channel --update
    - run: nix-build '<nixpkgs/nixos>' -A vm -I nixos-config=live-usb.nix --no-out-link
